<Alloy>
	<Window class="container" layout="vertical">
		<Label id="sectionTitle">section title</Label>
		
		<ScrollableView>
			<View>
				<ImageView id="postImage" image="{image}"/>
			
				<View id="postTitleBar">
					<Label id="postTitle" text="{name}"/>
				</View>
			</View>
		</ScrollableView>
	</Window>
</Alloy>

<!--jly-->

